import React from 'react'

/**
 * Renderiza uma tela genérica para quando uma determinada rota acessada não for encontrada
 * @author rafaelvictor01
 * @returns JSX.Element
 */
export default function NotFoundPage() {
    return (
        <div>
            <h1>Página não encontrada</h1>
        </div>
    )
}
